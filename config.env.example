# AROI Validator Deployment Configuration
# Copy this file to config.env and edit with your values

# Server IP address (where Caddy will bind)
DEPLOY_IP=YOUR_IP_ADDRESS

# Domain name for the validator
DEPLOY_DOMAIN=your-subdomain.your-domain.com

# Email for Let's Encrypt notifications
DEPLOY_EMAIL=admin@your-domain.com

# Cloudflare SSL mode (flexible or full-strict)
# Use 'flexible' if other services on domain don't support SSL
# Use 'full-strict' for better security if all services support SSL
CLOUDFLARE_MODE=flexible

